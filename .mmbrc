const path = require('path');

module.exports =
{
  "plugins": [
    [path.resolve(__dirname, "packages/plugin-transform-arrow-functions")],
    [path.resolve(__dirname, "packages/plugin-transform-modules-commonjs"), { "noInterop": true, "loose": true }]
  ]
}
