{
  "name": "my-module-bundler",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "bundler:dev": "node packages/bundler/src/index.js",
    "fmt": "prettier --write **/src/**/*.js",
    "test": "jest",
    "test:debug": "node --inspect jest --runInBand"
  },
  "devDependencies": {
    "jest": "^26.0.1",
    "prettier": "^2.0.5"
  },
  "workspaces": [
    "packages/*"
  ]
}
